[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function

# Add command-line options here if needed
# Example: addopts = -ra -q

# Register custom markers to avoid warnings
markers =
    integration: mark a test as an integration test (potentially slow or requiring external resources)

# Configure other pytest settings below 